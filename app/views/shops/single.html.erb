<div class="carousel-inner" role="listbox">
  <div class="item">
    <%= link_to image_tag(@shop.img_url, :border => 0, :alt => @shop.name), @shop.url, :target => '_blank' %>
    <div class="carousel-caption">
      <label><%= link_to @shop.name, @shop.url, :target => '_blank' %></label>
      <label class="keywords"><%= @shop.comment %></label>
    </div>
  </div>
</div>

