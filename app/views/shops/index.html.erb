<h1><label><%= @loc_name %>近くの</label><label><%= @menu_name %></label>の候補店一覧</h1>

<div class="list-group">
  <% @shops.each do |shop| %>
    <a href="<%= shop.url %>" class="list-group-item">
      <h4 class="list-group-item-heading"><%= shop.name %></h4>
    </a>
  <% end %>
</div>
